#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    setlocale(LC_ALL, "russian");
    //первая задача
    /*std::cout << "Klim\n";*/
    
    //вторая задача
    /*int a, b;
    cin >> a >> b;
    cout << a + b << " " << a-b<<" "<< a*b << " "<< a%b<< endl;*/

    //третья задача
    /*double b, c;
    cin >> b >> c;
    cout << -c / b << endl;*/

    //четвертая задача
    /*double a, b, c, D;
    cin >> a >> b >> c;
    D = (b * b) - (4 * a * c);
    if (D > 0) {
        cout << ((-b + sqrt(D)) / 2*a) << " " << ((-b - sqrt(D) )/ 2*a) << endl;
    }
    else if (D < 0) {
        cout << "Решения нет" << endl;
    }
    else if (D == 0) {
        cout << (-b / (2 * a)) << endl;
    }*/

    //Пятая задача
   /* int day, lamp, curtains;
    cout << "введите данные дня, лампы и штор" << endl;
    cout << "0/1" << endl;
    cin >> day >> lamp >> curtains;
    if ((day == 1) && (curtains == 1) || (lamp ==1)) {
        cout << "В комнате светло" << endl;
    }
    else if ((day == 0) && (lamp == 0)) {
        cout << "В комнате темно" << endl;
    }*/
}
